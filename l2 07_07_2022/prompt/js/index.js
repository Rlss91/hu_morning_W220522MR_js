var age = prompt("please enter your age");
console.log(age);

/*
    prompt can return 
    1) string with the user input
    2) string empty ""
    3) if the user press on cancel then prompt will return null
*/

//if the return value is not null and not empty string
//this mean that the user write something and press on ok btn
if (age != null && age != "") {
}
